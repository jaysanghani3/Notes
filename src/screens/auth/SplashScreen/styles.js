import {StyleSheet} from 'react-native';
import Colors from '../../../utils/Colors';

const styles = StyleSheet.create({
  fundwave: {fontSize: 50, color: Colors.fundwave, fontWeight: '800'},
});

export default styles;
